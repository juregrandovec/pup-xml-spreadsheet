version: '3.1'

services:
  app:
    build:
      args:
        user: root
        uid: 1
      context: ./
      dockerfile: Dockerfile
    image: web
    container_name: web
    restart: unless-stopped
    working_dir: /var/www/
    volumes:
      - ./application:/var/www